[tool.ruff]
line-length = 100
target-version = "py311"

# Keep this template strict enough to catch real issues, but not so strict it
# blocks early skeleton iteration.
select = ["E", "F", "I", "B"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["app"]
