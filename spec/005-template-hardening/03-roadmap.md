# 005 — Work Items Roadmap

Status: complete (see `02-execution.md`).

## Work items

- [x] Add `.env.example` and Compose env substitution.
- [x] Add CI to validate Compose + lint/build web + lint python.
- [x] Add Dependabot.
- [x] Add baseline linters (eslint for web, ruff for python).
- [x] Document template versioning at a high level.

## Notes

- CI uses `npm install` until a lockfile exists.
